import {Meta} from '@storybook/addon-docs/blocks';

<Meta title="Templates/Components/SearchTemplate"/>

# Default Search Template Factory

```jsx

const ComposedQuickSearch = R.compose(withQuickSearchCriteria, withQuickSearchFields)(QuickSearch);

const DefaultSearchTemplateFactory = props => {

	const {Component = SearchTemplate, ...otherProps} = props;

	const ComposedSearchTemplate = R.compose(withSearchTemplateConfigLoader)(Component);

	return (
		<ComposedSearchTemplate {...otherProps} QuickSearchComponent={ComposedQuickSearch}/>
	);
};
```