import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Templates/Actions/Create Case" />

# Create Case Action
 
```jsx
const CreateCaseAction = (props) => withActionView({
	...props,
	title: "New Case",
	ViewForm: CreateCaseViewForm,
	submitLabel: 'Create'
}, () => null);
```